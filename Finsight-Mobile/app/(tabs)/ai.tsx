import { View, Text } from 'react-native';

export default function AIScreen() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-textPrimary">AI Insights Screen</Text>
    </View>
  );
}
